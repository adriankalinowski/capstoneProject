spring:
  application:
    name: references
eureka:
  instance:
    hostname: localhost
    preferIpAddress: false
  client:
    registerWithEureka: true
    enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/